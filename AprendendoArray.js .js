// Arrays
// Ele sempre começa a contar do 0
let vasco = ['A', 'B', 'C', 'D'];
//EX: 
//     0 = A
//     1 = B
//     2 = C
//     3 = D
//Seria basicamente isso.
// Array também faz parte dos objetos 

//(const impede que a referência possa apontar para outro Array. OBS: Se tentar reinicializar a referência, dá erro de sintaxe!!!)
// var permite que a referência (variável) possa apontar para outro Array (serreinicializada).
//const e var permitem que o conteúdo do Array possa ser alterado, tanto em termosde quantidade quanto em termos de valores.
let familia = [26, 45, 'Ryan', 17];
console.log(familia.lenght);
console.log(familia[0]);
let nomeDoColega